# HR Platform Styles

Це повна система стилів для HR платформи, створена на чистому CSS без препроцесорів та бібліотек. Система містить понад 10,000 рядків CSS коду з неймовірно красивим дизайном.

## 📁 Структура файлів

```
styles/
├── index.css          # Головний файл, що імпортує всі стилі
├── base.css           # Базові стилі, CSS змінні, скидання браузерних стилів
├── layout.css         # Layout компоненти, grid система, header, sidebar, footer
├── components.css     # UI компоненти: кнопки, форми, картки, модалки
├── tables.css         # Стилі для таблиць та дата-тейблів
├── pages.css          # Специфічні стилі для сторінок (dashboard, candidates, jobs)
├── auth.css           # Стилі для аутентифікації та форм входу
├── animations.css     # Анімації, переходи, hover ефекти
├── utilities.css      # Утилітарні класи (spacing, colors, typography)
└── README.md          # Цей файл
```

## 🎨 Дизайн система

### Кольори
- **Primary**: Синій градієнт для основних елементів
- **Secondary**: Нейтральні кольори для тексту та фону
- **Success**: Зелений для позитивних дій
- **Warning**: Жовтий для попереджень
- **Error**: Червоний для помилок

### Типографіка
- Система розмірів шрифтів від `xs` до `9xl`
- Різні ваги шрифтів від `thin` до `black`
- Оптимізовані line-height для читабельності

### Spacing
- Консистентна система відступів від `0` до `96`
- Використання CSS змінних для легкої кастомізації

### Тіні
- 7 рівнів тіней від `sm` до `2xl`
- Плавні переходи між рівнями

## 🚀 Основні компоненти

### Кнопки
```css
.btn.btn-primary          /* Основна кнопка */
.btn.btn-secondary       /* Вторинна кнопка */
.btn.btn-success         /* Кнопка успіху */
.btn.btn-warning         /* Кнопка попередження */
.btn.btn-error           /* Кнопка помилки */
.btn.btn-ghost           /* Прозора кнопка */
.btn.btn-outline         /* Кнопка з обводкою */
```

### Форми
```css
.form-input              /* Поле вводу */
.form-textarea           /* Текстове поле */
.form-select             /* Випадаючий список */
.form-checkbox           /* Чекбокс */
.form-radio              /* Радіо кнопка */
```

### Картки
```css
.card                    /* Базова картка */
.card-primary            /* Картка з primary стилем */
.card-success            /* Картка з success стилем */
.card-warning            /* Картка з warning стилем */
.card-error              /* Картка з error стилем */
```

### Таблиці
```css
.table                   /* Базова таблиця */
.table-striped           /* Таблиця з смугами */
.table-hover             /* Таблиця з hover ефектами */
.table-sortable          /* Сортувана таблиця */
```

## 📱 Responsive дизайн

Система повністю адаптивна з breakpoints:
- **Mobile**: до 480px
- **Tablet**: до 768px  
- **Desktop**: до 1024px
- **Large Desktop**: до 1280px
- **2XL Desktop**: до 1536px

## ✨ Анімації

### Базові анімації
```css
.animate-fade-in         /* Поява з fade ефектом */
.animate-slide-in-up     /* Поява знизу */
.animate-scale-in        /* Поява з масштабуванням */
.animate-bounce          /* Підстрибування */
.animate-pulse           /* Пульсація */
```

### Hover ефекти
```css
.hover-lift              /* Підняття при hover */
.hover-scale             /* Масштабування при hover */
.hover-glow              /* Світіння при hover */
.hover-bounce            /* Підстрибування при hover */
```

### Stagger анімації
```css
.stagger-children        /* Анімація дітей з затримкою */
```

## 🎯 Утиліти

### Spacing
```css
.m-4                     /* margin: 1rem */
.p-6                     /* padding: 1.5rem */
.mx-auto                 /* margin: 0 auto */
.space-x-4               /* gap між елементами */
```

### Typography
```css
.text-xl                 /* font-size: 1.25rem */
.font-bold               /* font-weight: 700 */
.text-center             /* text-align: center */
.leading-relaxed         /* line-height: 1.625 */
```

### Colors
```css
.text-primary            /* color: var(--primary-600) */
.bg-secondary-50         /* background: var(--secondary-50) */
.border-success          /* border-color: var(--success-600) */
```

### Layout
```css
.flex                    /* display: flex */
.grid-cols-3             /* grid-template-columns: repeat(3, 1fr) */
.hidden                  /* display: none */
.relative                /* position: relative */
```

## 🔧 Кастомізація

### CSS змінні
Всі кольори, розміри та інші значення визначені як CSS змінні в `base.css`:

```css
:root {
  --primary-500: #3b82f6;
  --space-4: 1rem;
  --font-size-xl: 1.25rem;
  --radius-lg: 0.5rem;
}
```

### Темна тема
Система підтримує темну тему через `prefers-color-scheme`:

```css
@media (prefers-color-scheme: dark) {
  /* Автоматичні стилі для темної теми */
}
```

## 📋 Використання

### 1. Імпорт стилів
В головному файлі вашого додатку:

```typescript
import './styles/index.css';
```

### 2. Використання класів
```tsx
// Кнопка
<button className="btn btn-primary">
  Натисни мене
</button>

// Картка
<div className="card">
  <div className="card-header">
    <h3 className="card-title">Заголовок</h3>
  </div>
  <div className="card-body">
    Контент картки
  </div>
</div>

// Таблиця
<table className="table table-striped table-hover">
  {/* Вміст таблиці */}
</table>
```

### 3. Комбінація класів
```tsx
<div className="card hover-lift animate-fade-in">
  <div className="p-6 text-center">
    <h2 className="text-2xl font-bold text-primary mb-4">
      Заголовок
    </h2>
  </div>
</div>
```

## 🎨 Особливості дизайну

- **Градієнти**: Красиві градієнти для кнопок та фонів
- **Тіні**: Багаторівнева система тіней для глибини
- **Переходи**: Плавні анімації для всіх інтерактивних елементів
- **Backdrop blur**: Сучасні ефекти для модалок та header
- **Hover ефекти**: Інтерактивні ефекти при наведенні
- **Responsive**: Повна адаптивність для всіх пристроїв

## 🚀 Продуктивність

- **CSS змінні**: Швидка зміна теми без перекомпіляції
- **Оптимізовані селектори**: Ефективні CSS правила
- **Мінімальний CSS**: Тільки необхідні стилі
- **Браузерна оптимізація**: Використання нативних CSS можливостей

## 🔍 Підтримувані браузери

- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

## 📝 Ліцензія

Ці стилі створені для HR платформи та можуть використовуватися вільно в рамках проекту.

---

**Створено з ❤️ для неймовірно красивої HR платформи**
