# 🚀 TalentFlow Next.js - Startup Scripts

Цей документ описує як запустити TalentFlow Next.js проект разом з бекендом одним скриптом.

## 📁 Структура скриптів

- **`start-next.sh`** - Bash скрипт для macOS/Linux
- **`start-next.bat`** - Batch файл для Windows
- **`start.sh`** - Загальний скрипт для запуску всього проекту

## 🎯 Що роблять скрипти

1. **Перевіряють залежності** - Node.js, npm
2. **Очищають порти** - автоматично зупиняють процеси на портах 3000 та 3002
3. **Встановлюють залежності** - якщо потрібно
4. **Шукають бекенд** - в різних директоріях
5. **Запускають бекенд** - на порту 3002
6. **Запускають Next.js** - на порту 3000
7. **Моніторять процеси** - перевіряють статус

## 🚀 Швидкий запуск

### macOS/Linux:
```bash
cd TalentFlow/talentflow-next
./start-next.sh
```

### Windows:
```cmd
cd TalentFlow\talentflow-next
start-next.bat
```

## 📋 Вимоги

- **Node.js** версія 18+ 
- **npm** або **yarn**
- **Backend** з `package.json` та скриптами `dev` або `start`

## 🔍 Пошук бекенду

Скрипти автоматично шукають бекенд в наступних директоріях:

```
TalentFlow/
├── talentflow-next/     ← Next.js фронтенд (порт 3000)
├── backend/            ← Бекенд (порт 3002)
├── api/                ← Альтернативна назва
├── server/             ← Альтернативна назва
└── TalentFlow-backend/ ← Альтернативна назва
```

## 📱 Порти за замовчуванням

- **Next.js Frontend**: `http://localhost:3000`
- **Backend API**: `http://localhost:3002`
- **Backend Health**: `http://localhost:3002/health`

## 🛠️ Як це працює

### 1. Перевірка проекту
```bash
# Перевіряє чи є package.json
# Перевіряє чи це Next.js проект
# Перевіряє версію Node.js
```

### 2. Очищення портів
```bash
# Автоматично зупиняє процеси на портах 3000 та 3002
# Забезпечує чистий запуск
```

### 3. Встановлення залежностей
```bash
# Встановлює frontend залежності
# Встановлює backend залежності (якщо знайдено)
```

### 4. Запуск сервісів
```bash
# Запускає backend на порту 3002 (PORT=3002 npm run dev)
# Запускає Next.js на порту 3000 (npm run dev)
# Моніторить процеси
```

### 5. Моніторинг
```bash
# Перевіряє чи процеси працюють
# Показує статус та порти
# Очищає при завершенні
```

## 🔧 Налаштування

### Зміна портів
Скрипти автоматично налаштовують:
- **Backend**: `PORT=3002`
- **Frontend**: `PORT=3000` (за замовчуванням Next.js)

Якщо потрібно змінити порти, відредагуйте скрипти:
```bash
# В start-next.sh
export PORT=3002  # Backend port
# Frontend залишається на 3000
```

### Додавання нових шляхів до бекенду
В `start-next.sh`:
```bash
BACKEND_PATHS=(
    "../backend"
    "../api"
    "../server"
    "backend"
    "api"
    "server"
    "../TalentFlow-backend"
    "../talentflow-backend"
    "../my-custom-backend"  # ← Додати новий шлях
)
```

## 🚨 Рішення проблем

### Помилка "Permission denied"
```bash
chmod +x start-next.sh
```

### Помилка "Node.js not found"
Встановіть Node.js з [офіційного сайту](https://nodejs.org/)

### Помилка "npm not found"
Встановіть npm разом з Node.js

### Помилка "address already in use"
Скрипти автоматично вирішують цю проблему:
- Автоматично зупиняють процеси на портах 3000 та 3002
- Backend запускається на порт 3002
- Frontend запускається на порт 3000

### Бекенд не знайдено
Переконайтеся що:
- Бекенд має `package.json`
- В `package.json` є скрипти `dev` або `start`
- Бекенд знаходиться в одній з пошукових директорій

### Порт зайнятий
Скрипти автоматично налаштовують правильні порти та очищають їх:
- Backend: 3002 (автоматично очищається)
- Frontend: 3000 (автоматично очищається)

## 📊 Логи та діагностика

### Перевірка статусу процесів
```bash
# Перевірити чи Next.js працює
curl http://localhost:3000

# Перевірити чи бекенд працює
curl http://localhost:3002/health
```

### Перевірка портів
```bash
# macOS/Linux
lsof -i :3000  # Frontend
lsof -i :3002  # Backend

# Windows
netstat -an | findstr :3000
netstat -an | findstr :3002
```

## 🎉 Результат

Після успішного запуску ви побачите:

```
==============================================
🎉 Application Status:
✅ Next.js Frontend: http://localhost:3000 (PID: 12345)
✅ Backend: Running on port 3002 (PID: 12346)
🔗 Backend API: http://localhost:3002
🔗 Backend Health: http://localhost:3002/health
==============================================
📝 Press Ctrl+C to stop all processes
🔄 Monitoring processes...
```

## 🛑 Зупинка

Натисніть `Ctrl+C` щоб зупинити всі процеси. Скрипт автоматично очистить всі запущені процеси.

## 🔄 Перезапуск

Для перезапуску:
1. Зупиніть поточні процеси (`Ctrl+C`)
2. Запустіть скрипт знову

## 📝 Примітки

- Скрипти автоматично встановлюють залежності при першому запуску
- **Автоматично очищають порти** перед запуском
- Backend автоматично запускається на порту 3002
- Frontend залишається на порту 3000
- Всі процеси моніторяться та автоматично очищаються при завершенні
- Скрипти працюють як в macOS/Linux так і в Windows
- Конфлікт портів вирішується автоматично
- **Нова функція**: Автоматична очистка портів для безпроблемного запуску

---

**🚀 Happy coding with TalentFlow!**

Цей документ описує як запустити TalentFlow Next.js проект разом з бекендом одним скриптом.

## 📁 Структура скриптів

- **`start-next.sh`** - Bash скрипт для macOS/Linux
- **`start-next.bat`** - Batch файл для Windows
- **`start.sh`** - Загальний скрипт для запуску всього проекту

## 🎯 Що роблять скрипти

1. **Перевіряють залежності** - Node.js, npm
2. **Очищають порти** - автоматично зупиняють процеси на портах 3000 та 3002
3. **Встановлюють залежності** - якщо потрібно
4. **Шукають бекенд** - в різних директоріях
5. **Запускають бекенд** - на порту 3002
6. **Запускають Next.js** - на порту 3000
7. **Моніторять процеси** - перевіряють статус

## 🚀 Швидкий запуск

### macOS/Linux:
```bash
cd TalentFlow/talentflow-next
./start-next.sh
```

### Windows:
```cmd
cd TalentFlow\talentflow-next
start-next.bat
```

## 📋 Вимоги

- **Node.js** версія 18+ 
- **npm** або **yarn**
- **Backend** з `package.json` та скриптами `dev` або `start`

## 🔍 Пошук бекенду

Скрипти автоматично шукають бекенд в наступних директоріях:

```
TalentFlow/
├── talentflow-next/     ← Next.js фронтенд (порт 3000)
├── backend/            ← Бекенд (порт 3002)
├── api/                ← Альтернативна назва
├── server/             ← Альтернативна назва
└── TalentFlow-backend/ ← Альтернативна назва
```

## 📱 Порти за замовчуванням

- **Next.js Frontend**: `http://localhost:3000`
- **Backend API**: `http://localhost:3002`
- **Backend Health**: `http://localhost:3002/health`

## 🛠️ Як це працює

### 1. Перевірка проекту
```bash
# Перевіряє чи є package.json
# Перевіряє чи це Next.js проект
# Перевіряє версію Node.js
```

### 2. Очищення портів
```bash
# Автоматично зупиняє процеси на портах 3000 та 3002
# Забезпечує чистий запуск
```

### 3. Встановлення залежностей
```bash
# Встановлює frontend залежності
# Встановлює backend залежності (якщо знайдено)
```

### 4. Запуск сервісів
```bash
# Запускає backend на порту 3002 (PORT=3002 npm run dev)
# Запускає Next.js на порту 3000 (npm run dev)
# Моніторить процеси
```

### 5. Моніторинг
```bash
# Перевіряє чи процеси працюють
# Показує статус та порти
# Очищає при завершенні
```

## 🔧 Налаштування

### Зміна портів
Скрипти автоматично налаштовують:
- **Backend**: `PORT=3002`
- **Frontend**: `PORT=3000` (за замовчуванням Next.js)

Якщо потрібно змінити порти, відредагуйте скрипти:
```bash
# В start-next.sh
export PORT=3002  # Backend port
# Frontend залишається на 3000
```

### Додавання нових шляхів до бекенду
В `start-next.sh`:
```bash
BACKEND_PATHS=(
    "../backend"
    "../api"
    "../server"
    "backend"
    "api"
    "server"
    "../TalentFlow-backend"
    "../talentflow-backend"
    "../my-custom-backend"  # ← Додати новий шлях
)
```

## 🚨 Рішення проблем

### Помилка "Permission denied"
```bash
chmod +x start-next.sh
```

### Помилка "Node.js not found"
Встановіть Node.js з [офіційного сайту](https://nodejs.org/)

### Помилка "npm not found"
Встановіть npm разом з Node.js

### Помилка "address already in use"
Скрипти автоматично вирішують цю проблему:
- Автоматично зупиняють процеси на портах 3000 та 3002
- Backend запускається на порт 3002
- Frontend запускається на порт 3000

### Бекенд не знайдено
Переконайтеся що:
- Бекенд має `package.json`
- В `package.json` є скрипти `dev` або `start`
- Бекенд знаходиться в одній з пошукових директорій

### Порт зайнятий
Скрипти автоматично налаштовують правильні порти та очищають їх:
- Backend: 3002 (автоматично очищається)
- Frontend: 3000 (автоматично очищається)

## 📊 Логи та діагностика

### Перевірка статусу процесів
```bash
# Перевірити чи Next.js працює
curl http://localhost:3000

# Перевірити чи бекенд працює
curl http://localhost:3002/health
```

### Перевірка портів
```bash
# macOS/Linux
lsof -i :3000  # Frontend
lsof -i :3002  # Backend

# Windows
netstat -an | findstr :3000
netstat -an | findstr :3002
```

## 🎉 Результат

Після успішного запуску ви побачите:

```
==============================================
🎉 Application Status:
✅ Next.js Frontend: http://localhost:3000 (PID: 12345)
✅ Backend: Running on port 3002 (PID: 12346)
🔗 Backend API: http://localhost:3002
🔗 Backend Health: http://localhost:3002/health
==============================================
📝 Press Ctrl+C to stop all processes
🔄 Monitoring processes...
```

## 🛑 Зупинка

Натисніть `Ctrl+C` щоб зупинити всі процеси. Скрипт автоматично очистить всі запущені процеси.

## 🔄 Перезапуск

Для перезапуску:
1. Зупиніть поточні процеси (`Ctrl+C`)
2. Запустіть скрипт знову

## 📝 Примітки

- Скрипти автоматично встановлюють залежності при першому запуску
- **Автоматично очищають порти** перед запуском
- Backend автоматично запускається на порту 3002
- Frontend залишається на порту 3000
- Всі процеси моніторяться та автоматично очищаються при завершенні
- Скрипти працюють як в macOS/Linux так і в Windows
- Конфлікт портів вирішується автоматично
- **Нова функція**: Автоматична очистка портів для безпроблемного запуску

---

**🚀 Happy coding with TalentFlow!**
