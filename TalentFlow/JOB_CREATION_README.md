# 🚀 Job Creation System - TalentFlow

Повна система створення та управління вакансіями для платформи TalentFlow.

## ✨ Функціональність

### Frontend
- **CreateJobModal** - Модальне вікно створення вакансії
- **Dashboard Integration** - Інтеграція з головною сторінкою
- **Responsive Design** - Адаптивний дизайн для всіх пристроїв
- **Preview Mode** - Режим перегляду перед створенням
- **Form Validation** - Валідація форми на клієнті

### Backend
- **Job Model** - Повна модель вакансії з TypeORM
- **Job Service** - Бізнес-логіка для роботи з вакансіями
- **Job Controller** - API контролер з Express
- **Validation Middleware** - Валідація даних на сервері
- **Database Migration** - Міграція для таблиці вакансій

## 🏗️ Архітектура

### Frontend Structure
```
web/src/
├── components/UI/
│   └── CreateJobModal.tsx      # Модальне вікно створення
├── services/
│   └── jobService.ts           # API сервіс для вакансій
├── styles/
│   └── job-creation.css        # Стилі для модального вікна
└── pages/
    └── DashboardPage.tsx       # Інтеграція з дашбордом
```

### Backend Structure
```
backend/src/
├── models/
│   └── Job.ts                  # Модель вакансії
├── services/
│   └── JobService.ts           # Сервіс для роботи з вакансіями
├── controllers/
│   └── jobController.ts        # API контролер
├── repositories/
│   └── JobRepository.ts        # Репозиторій для БД
├── middleware/
│   └── validation.ts           # Валідація даних
├── routes/
│   └── jobRoutes.ts            # API роути
└── database/migrations/
    └── 1700000000003-CreateJobsTable.ts
```

## 🚀 Швидкий старт

### 1. Запуск Backend
```bash
cd backend
npm install
npm run dev
```

### 2. Запуск Frontend
```bash
cd web
npm install
npm run dev
```

### 3. Запуск міграції
```bash
cd backend
npm run db:migrate
```

## 📋 API Endpoints

### Створення вакансії
```http
POST /api/jobs
Authorization: Bearer <token>
Content-Type: application/json

{
  "title": "Senior Full-Stack Developer",
  "description": "Детальний опис вакансії...",
  "type": "full_time",
  "experienceLevel": "senior",
  "location": "Київ, Україна",
  "salaryMin": 3000,
  "salaryMax": 5000,
  "currency": "USD"
}
```

### Отримання вакансій
```http
GET /api/jobs?page=1&limit=20&type=full_time&location=Київ
```

### Оновлення вакансії
```http
PUT /api/jobs/:id
Authorization: Bearer <token>
```

### Публікація вакансії
```http
PATCH /api/jobs/:id/publish
Authorization: Bearer <token>
```

## 🎨 UI Components

### CreateJobModal
- **Форма створення** з усіма необхідними полями
- **Режим перегляду** для перевірки перед створенням
- **Валідація** на клієнті та сервері
- **Responsive дизайн** для всіх пристроїв

### Поля форми
- **Основна інформація**: назва, опис, відділ
- **Деталі**: тип, рівень досвіду, локація
- **Зарплата**: мін/макс, валюта
- **Навички та теги**: динамічне додавання
- **Додаткові опції**: терміновість, рекомендація, дедлайн

## 🔧 Технічні деталі

### Database Schema
```sql
CREATE TABLE jobs (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  title VARCHAR(255) NOT NULL,
  description TEXT NOT NULL,
  requirements TEXT,
  benefits TEXT,
  location VARCHAR(255),
  remote VARCHAR(255),
  type ENUM('full_time', 'part_time', 'contract', 'internship', 'freelance'),
  experienceLevel ENUM('entry', 'junior', 'middle', 'senior', 'lead', 'executive'),
  salaryMin DECIMAL(10,2),
  salaryMax DECIMAL(10,2),
  currency VARCHAR(10),
  department VARCHAR(255),
  skills TEXT, -- JSON array
  tags TEXT, -- JSON array
  status ENUM('draft', 'active', 'paused', 'closed', 'expired'),
  isUrgent BOOLEAN DEFAULT false,
  isFeatured BOOLEAN DEFAULT false,
  deadline TIMESTAMP,
  views INTEGER DEFAULT 0,
  applications INTEGER DEFAULT 0,
  employerId UUID NOT NULL,
  createdAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updatedAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  publishedAt TIMESTAMP,
  closedAt TIMESTAMP
);
```

### Validation Rules
- **Назва**: мінімум 3 символи
- **Опис**: мінімум 20 символів
- **Зарплата**: мін ≤ макс
- **Дедлайн**: має бути в майбутньому
- **Навички/теги**: не можуть бути порожніми

## 🎯 Використання

### 1. Відкриття модального вікна
Натисніть кнопку "Створити вакансію" на дашборді.

### 2. Заповнення форми
- Заповніть обов'язкові поля (позначені *)
- Додайте навички та теги
- Встановіть зарплату та дедлайн

### 3. Перегляд
Переключіться в режим перегляду для перевірки.

### 4. Створення
Натисніть "Створити вакансію" для збереження.

## 🔒 Безпека

- **Аутентифікація** через JWT токени
- **Валідація** даних на клієнті та сервері
- **Авторизація** - тільки роботодавці можуть створювати вакансії
- **SQL Injection Protection** через TypeORM

## 🚀 Розширення

### Додавання нових полів
1. Оновіть модель `Job.ts`
2. Додайте валідацію в `validation.ts`
3. Оновіть міграцію
4. Додайте поля в `CreateJobModal.tsx`

### Додавання нових статусів
1. Оновіть enum `JobStatus`
2. Додайте відповідні методи в `JobService`
3. Створіть нові API endpoints

## 📱 Responsive Design

- **Desktop**: Повна форма з двома колонками
- **Tablet**: Адаптивна сітка
- **Mobile**: Одноколонкова форма, повноекранне модальне вікно

## 🎨 Стилізація

### CSS Variables
```css
:root {
  --color-primary-500: #3b82f6;
  --color-success-500: #22c55e;
  --color-warning-500: #f59e0b;
  --color-error-500: #ef4444;
}
```

### Tailwind CSS
Використовується Tailwind CSS для базових стилів та кастомні CSS для специфічних компонентів.

## 🔍 Debugging

### Frontend
- Перевірте консоль браузера
- Використовуйте React DevTools
- Перевірте Network tab для API запитів

### Backend
- Логи в консолі сервера
- Перевірте базу даних
- Використовуйте Postman для тестування API

## 📚 Документація

- **TypeScript**: Повна типізація
- **JSDoc**: Коментарі для функцій
- **API Docs**: Swagger документація
- **Code Examples**: Приклади використання

## 🤝 Contributing

1. Fork репозиторій
2. Створіть feature branch
3. Зробіть коміт змін
4. Push в branch
5. Створіть Pull Request

## 📄 Ліцензія

MIT License - дивіться файл LICENSE для деталей.

---

**TalentFlow Team** 🚀
*Створюйте майбутнє найму разом з нами!*
