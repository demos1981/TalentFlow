# 🤖 AI Matching API Documentation

## 📋 Огляд

AI Matching API надає можливість автоматично генерувати та керувати рекомендаціями для матчингу кандидатів та вакансій за допомогою штучного інтелекту.

## 🚀 Основні можливості

- **Автоматичний матчинг** кандидатів та вакансій
- **AI алгоритм** для розрахунку балів сумісності
- **Фільтрація та пошук** рекомендацій
- **Статистика та аналітика** матчингу
- **Відстеження статусу** рекомендацій
- **Відгуки користувачів** про якість матчингу

## 🏗️ Архітектура

### Моделі даних

#### AiRecommendation
```typescript
{
  id: string;                    // UUID рекомендації
  type: RecommendationType;      // Тип: candidate_to_job або job_to_candidate
  candidateId: string;           // ID кандидата
  jobId: string;                 // ID вакансії
  matchScore: number;            // Бал матчингу (0-100)
  matchScoreCategory: MatchScore; // Категорія: excellent, good, average, poor
  skillsMatch: {                 // Матчинг по навичках
    matched: string[];
    missing: string[];
    score: number;
  };
  experienceMatch: {             // Матчинг по досвіду
    required: string;
    candidate: string;
    score: number;
  };
  locationMatch: {               // Матчинг по локації
    required: string;
    candidate: string;
    score: number;
  };
  salaryMatch: {                 // Матчинг по зарплаті
    required: { min: number; max: number };
    candidate: { min: number; max: number };
    score: number;
  };
  aiReason: string;              // AI пояснення матчингу
  aiMetadata: {                  // Метадані AI моделі
    model: string;
    confidence: number;
    processingTime: number;
    features: string[];
  };
  isActive: boolean;             // Чи активна рекомендація
  isViewed: boolean;             // Чи переглянута
  isContacted: boolean;          // Чи був контакт
  feedback?: {                   // Відгук користувача
    rating: number;
    comment: string;
    createdAt: Date;
  };
  createdAt: Date;               // Дата створення
  updatedAt: Date;               // Дата оновлення
}
```

### Алгоритм матчингу

1. **Skills Match (40%)** - Порівняння навичок кандидата та вимог вакансії
2. **Experience Match (25%)** - Порівняння досвіду роботи
3. **Location Match (20%)** - Порівняння локації
4. **Salary Match (15%)** - Порівняння зарплатних очікувань

## 🔗 API Endpoints

### Base URL
```
http://localhost:3000/api/ai-matching
```

### 1. Отримати рекомендації
```http
GET /api/ai-matching/recommendations
```

**Query Parameters:**
- `search` (string) - Пошук по назві вакансії, компанії, імені кандидата
- `location` (string) - Фільтр по локації
- `experience` (string) - Фільтр по досвіду
- `salaryRange` (string) - Фільтр по зарплаті
- `skills` (string) - Навички через кому
- `minMatchScore` (number) - Мінімальний бал матчингу (0-100)
- `type` (string) - Тип рекомендації: `candidate_to_job` або `job_to_candidate`
- `candidateId` (string) - ID конкретного кандидата
- `jobId` (string) - ID конкретної вакансії
- `isActive` (boolean) - Фільтр по активності
- `limit` (number) - Кількість результатів (за замовчуванням: 20)
- `offset` (number) - Зміщення для пагінації (за замовчуванням: 0)

**Response:**
```json
{
  "success": true,
  "data": {
    "recommendations": [...],
    "stats": {...},
    "pagination": {
      "total": 150,
      "limit": 20,
      "offset": 0,
      "hasMore": true
    },
    "filters": {...}
  }
}
```

### 2. Згенерувати рекомендації
```http
POST /api/ai-matching/generate
```

**Request Body:**
```json
{
  "candidateId": "uuid",        // ID кандидата (обов'язково якщо немає jobId)
  "jobId": "uuid",              // ID вакансії (обов'язково якщо немає candidateId)
  "type": "candidate_to_job",   // Тип рекомендації
  "limit": 10,                  // Кількість рекомендацій
  "forceRegenerate": false      // Примусово перегенерувати
}
```

**Response:**
```json
{
  "success": true,
  "data": [...],
  "message": "Generated 10 recommendations"
}
```

### 3. Оновити рекомендацію
```http
PUT /api/ai-matching/recommendations/:id
```

**Request Body:**
```json
{
  "isViewed": true,             // Позначити як переглянуту
  "isContacted": true,          // Позначити як контактовану
  "feedbackRating": 5,          // Оцінка (1-5)
  "feedbackComment": "Відмінний матч!" // Коментар
}
```

**Response:**
```json
{
  "success": true,
  "data": {...},
  "message": "Recommendation updated successfully"
}
```

### 4. Отримати статистику
```http
GET /api/ai-matching/stats
```

**Response:**
```json
{
  "success": true,
  "data": {
    "totalMatches": 150,
    "highQualityMatches": 45,
    "averageMatchScore": 78.5,
    "candidatesMatched": 89,
    "jobsMatched": 67,
    "lastUpdated": "2024-01-15T10:30:00Z",
    "aiAccuracy": 94.5,
    "processingTime": 245,
    "matchScoreDistribution": {
      "excellent": 23,
      "good": 45,
      "average": 56,
      "poor": 26
    },
    "topSkills": [...],
    "topLocations": [...]
  }
}
```

### 5. Масово згенерувати рекомендації
```http
POST /api/ai-matching/bulk-generate
```

**Request Body:**
```json
{
  "type": "candidate_to_job",
  "limit": 50
}
```

### 6. Видалити застарілі рекомендації
```http
DELETE /api/ai-matching/cleanup?daysOld=30
```

**Query Parameters:**
- `daysOld` (number) - Кількість днів (за замовчуванням: 30)

### 7. Перевірка стану сервісу
```http
GET /api/ai-matching/health
```

**Response:**
```json
{
  "success": true,
  "data": {
    "status": "healthy",
    "timestamp": "2024-01-15T10:30:00Z",
    "totalMatches": 150,
    "averageScore": 78.5,
    "aiAccuracy": 94.5
  }
}
```

## 🔐 Аутентифікація

Всі ендпоінти потребують аутентифікації через `authMiddleware`. Передавайте JWT токен в заголовку:

```http
Authorization: Bearer <your-jwt-token>
```

## 📊 Приклади використання

### Отримати топ рекомендації для кандидата
```bash
curl -X GET "http://localhost:3000/api/ai-matching/recommendations?candidateId=123&minMatchScore=80&limit=10" \
  -H "Authorization: Bearer <token>"
```

### Згенерувати рекомендації для вакансії
```bash
curl -X POST "http://localhost:3000/api/ai-matching/generate" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <token>" \
  -d '{
    "jobId": "456",
    "type": "job_to_candidate",
    "limit": 15
  }'
```

### Оновити статус рекомендації
```bash
curl -X PUT "http://localhost:3000/api/ai-matching/recommendations/789" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <token>" \
  -d '{
    "isViewed": true,
    "feedbackRating": 5,
    "feedbackComment": "Ідеальний матч!"
  }'
```

## 🚀 Швидкий старт

### 1. Запустіть міграцію
```bash
chmod +x scripts/run-ai-matching-migration.sh
./scripts/run-ai-matching-migration.sh
```

### 2. Додайте роути в server.ts
```typescript
import aiMatchingRoutes from './routes/aiMatching';

// ... existing code ...

app.use('/api/ai-matching', aiMatchingRoutes);
```

### 3. Перезапустіть сервер
```bash
npm run dev
```

### 4. Тестуйте API
```bash
curl http://localhost:3000/api/ai-matching/health
```

## 📈 Моніторинг та логування

### Логи
Всі операції логуються в консоль з детальною інформацією про:
- Час виконання
- Кількість згенерованих рекомендацій
- Помилки та винятки
- Статистику використання

### Метрики
API надає детальну статистику:
- Загальна кількість матчів
- Середній бал матчингу
- Розподіл по категоріях
- Топ навички та локації
- Точність AI моделі

## 🔧 Налаштування

### Конфігурація AI моделі
В `AiMatchingService` можна налаштувати:
- Ваги для різних критеріїв матчингу
- Пороги для категорій балів
- Алгоритми порівняння навичок
- Логіку розрахунку локації

### Продуктивність
- Індекси на всіх ключових полях
- Пагінація для великих наборів даних
- Кешування статистики
- Асинхронна обробка масових операцій

## 🐛 Відомі проблеми

1. **Великі набори даних** - При генерації >1000 рекомендацій може займати багато часу
2. **Складні фільтри** - Деякі комбінації фільтрів можуть сповільнювати запити
3. **Матчинг навичок** - Точний матчинг потребує нормалізації назв навичок

## 🔮 Майбутні покращення

- [ ] Інтеграція з реальними AI моделями (OpenAI, Claude)
- [ ] Машинне навчання на основі відгуків користувачів
- [ ] Покращений алгоритм матчингу навичок
- [ ] Реал-тайм оновлення рекомендацій
- [ ] Аналітика ефективності матчингу
- [ ] Інтеграція з зовнішніми API (LinkedIn, Indeed)

## 📞 Підтримка

При виникненні проблем:
1. Перевірте логи сервера
2. Використайте `/health` ендпоінт
3. Перевірте підключення до бази даних
4. Запустіть міграції заново

---

**TalentFlow AI Matching API** - Де талант знаходить свою роботу! 🚀
