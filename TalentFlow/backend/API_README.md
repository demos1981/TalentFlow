# TalentFlow API Documentation

## 🚀 Огляд

TalentFlow - це AI-підсилена HR платформа для пошуку та управління талантами. API надає повний набір ендпоінтів для управління користувачами, вакансіями, кандидатами та аналітикою.

## 📋 Зміст

- [Базові URL](#базові-url)
- [Авторизація](#авторизація)
- [Основні ендпоінти](#основні-ендпоінти)
- [Приклади використання](#приклади-використання)
- [Коди відповідей](#коди-відповідей)
- [Swagger документація](#swagger-документація)

## 🌐 Базові URL

- **Development**: `http://localhost:${PORT}` (за замовчуванням 3000)
- **Production**: `https://api.talentflow.io`

> **Примітка**: Порт можна змінити через змінну середовища `PORT`

## 🔐 Авторизація

API використовує JWT токени для авторизації. Більшість ендпоінтів потребують заголовок:

```
Authorization: Bearer <your_jwt_token>
```

### Отримання токену

1. **Реєстрація** (POST `/api/auth/register`)
2. **Вхід** (POST `/api/auth/login`)
3. **Оновлення токену** (POST `/api/auth/refresh-token`)

## 📡 Основні ендпоінти

### 🔑 Аутентифікація

| Метод | Ендпоінт | Опис |
|-------|----------|------|
| POST | `/api/auth/register` | Реєстрація нового користувача |
| POST | `/api/auth/login` | Вхід в систему |
| POST | `/api/auth/refresh-token` | Оновлення JWT токену |
| POST | `/api/auth/change-password` | Зміна паролю (потребує авторизації) |
| POST | `/api/auth/verify-email/{userId}` | Верифікація email |

### 👥 Користувачі

| Метод | Ендпоінт | Опис |
|-------|----------|------|
| GET | `/api/users/profile` | Отримання профілю користувача |
| PUT | `/api/users/profile` | Оновлення профілю користувача |
| GET | `/api/users/{id}` | Отримання користувача за ID |

### 💼 Вакансії

| Метод | Ендпоінт | Опис |
|-------|----------|------|
| GET | `/api/jobs` | Список вакансій з фільтрами |
| POST | `/api/jobs` | Створення нової вакансії |
| GET | `/api/jobs/{id}` | Отримання вакансії за ID |
| PUT | `/api/jobs/{id}` | Оновлення вакансії |
| DELETE | `/api/jobs/{id}` | Видалення вакансії |

### 👨‍💼 Кандидати

| Метод | Ендпоінт | Опис |
|-------|----------|------|
| GET | `/api/candidates` | Список кандидатів з фільтрами |
| GET | `/api/candidates/{id}` | Отримання кандидата за ID |

### 📊 Дашборд

| Метод | Ендпоінт | Опис |
|-------|----------|------|
| GET | `/api/dashboard/stats` | Статистика дашборду |
| GET | `/api/dashboard/overview` | Загальний огляд платформи |

### 🔍 Пошук

| Метод | Ендпоінт | Опис |
|-------|----------|------|
| GET | `/api/search/jobs` | Пошук вакансій |
| GET | `/api/search/candidates` | Пошук кандидатів |

### 🏥 Система

| Метод | Ендпоінт | Опис |
|-------|----------|------|
| GET | `/health` | Перевірка стану сервера |

## 💡 Приклади використання

### Реєстрація користувача

```bash
curl -X POST http://localhost:${PORT}/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "password123",
    "firstName": "Іван",
    "lastName": "Петренко",
    "role": "candidate",
    "phone": "+380991234567"
  }'
```

### Вхід в систему

```bash
curl -X POST http://localhost:${PORT}/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "password123"
  }'
```

### Отримання списку вакансій

```bash
curl -X GET "http://localhost:${PORT}/api/jobs?page=1&limit=10&search=react&location=Київ" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

### Створення вакансії

```bash
curl -X POST http://localhost:${PORT}/api/jobs \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -d '{
    "title": "Senior React Developer",
    "description": "Шукаємо досвідченого React розробника...",
    "company": "TechCorp",
    "location": "Київ, Україна",
    "type": "full-time",
    "salary": {
      "min": 3000,
      "max": 5000,
      "currency": "USD"
    },
    "requirements": ["React", "TypeScript", "5+ years experience"],
    "benefits": ["Remote work", "Health insurance", "Flexible hours"]
  }'
```

### Отримання статистики

```bash
curl -X GET http://localhost:${PORT}/api/dashboard/stats \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

## 📊 Коди відповідей

| Код | Опис |
|-----|------|
| 200 | Успішна операція |
| 201 | Ресурс створено |
| 400 | Помилка валідації |
| 401 | Необхідна авторизація |
| 403 | Недостатньо прав |
| 404 | Ресурс не знайдено |
| 500 | Внутрішня помилка сервера |

## 📚 Swagger документація

Повна інтерактивна документація доступна за адресою:

**Development**: http://localhost:${PORT}/api-docs

Swagger UI надає:
- ✅ Інтерактивне тестування ендпоінтів
- ✅ Детальні схеми запитів та відповідей
- ✅ Приклади використання
- ✅ Автоматичну авторизацію через JWT токени

## 🔧 Технології

- **Node.js** - серверна платформа
- **Express.js** - веб-фреймворк
- **TypeORM** - ORM для роботи з базою даних
- **PostgreSQL** - база даних
- **JWT** - авторизація
- **Swagger/OpenAPI** - документація API

## 🚀 Швидкий старт

1. **Клонування репозиторію**
   ```bash
   git clone <repository-url>
   cd hr_platform/backend
   ```

2. **Встановлення залежностей**
   ```bash
   npm install
   ```

3. **Налаштування змінних середовища**
   ```bash
   cp env.example .env
   # Відредагуйте .env файл
   
   # Основні змінні:
   PORT=3000                    # Порт сервера
   DATABASE_URL=postgresql://... # URL бази даних
   JWT_SECRET=your_jwt_secret   # Секрет для JWT токенів
   ```

4. **Запуск сервера**
   ```bash
   npm run dev
   ```

5. **Відкриття документації**
   ```
   http://localhost:3000/api-docs
   ```

## 📞 Підтримка

- **Email**: hello@talentflow.io
- **Документація**: http://localhost:${PORT}/api-docs
- **GitHub**: [repository-url]

---

© 2024 TalentFlow. Всі права захищені.
