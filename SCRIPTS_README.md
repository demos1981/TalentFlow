# 🚀 TalentFlow - Скрипти управління

Зручні скрипти для швидкого запуску, зупинки та моніторингу проекту TalentFlow.

## 📋 Доступні скрипти

### 🚀 `start.sh` - Запуск проекту
Запускає весь проект TalentFlow одним командою.

```bash
./start.sh
```

**Що робить:**
- ✅ Перевіряє залежності (Node.js 20+, npm)
- ✅ Встановлює npm пакети для backend та frontend
- ✅ Створює .env файли з прикладів
- ✅ Запускає PostgreSQL (через Docker або локально)
- ✅ Виконує міграції бази даних
- ✅ Запускає backend сервер
- ✅ Запускає frontend сервер
- ✅ Автоматично знаходить порти
- ✅ Показує корисні посилання

**Результат:**
```
================================
🎉 TalentFlow Ready!
================================

📊 Статус сервісів:
  Backend:  ✅ http://localhost:3000
  Frontend: ✅ http://localhost:3001

🔗 Корисні посилання:
  API:      http://localhost:3000
  Health:   http://localhost:3000/health
  Swagger:  http://localhost:3000/api-docs
  Website:  http://localhost:3001

🎯 Для тестування відкрийте:
  http://localhost:3001

🛠️ Команди для управління:
  Зупинити: Ctrl+C
  Логи:     tail -f backend/logs/app.log
  Тест API: curl http://localhost:3000/health

📱 TalentFlow готовий до роботи!
Where Talent Flows Naturally 🚀
```

### 🛑 `stop.sh` - Зупинка проекту
Зупиняє всі сервіси TalentFlow.

```bash
./stop.sh
```

**Що робить:**
- ✅ Зупиняє backend процеси
- ✅ Зупиняє frontend процеси
- ✅ Зупиняє Vite процеси
- ✅ Зупиняє ts-node-dev процеси
- ✅ Зупиняє процеси на портах 3000-3010

**Результат:**
```
================================
🛑 TalentFlow Stop Script
================================

[INFO] Зупиняю всі сервіси TalentFlow...
[INFO] Зупиняю Node.js процеси...
[INFO] Зупиняю backend процеси: 12345
[SUCCESS] Backend процеси зупинено
[INFO] Зупиняю frontend процеси: 12346
[SUCCESS] Frontend процеси зупинено
[SUCCESS] Всі сервіси TalentFlow зупинено!

================================
✅ TalentFlow зупинено
================================

💡 Для запуску знову виконайте:
  ./start.sh
```

### 📊 `status.sh` - Перевірка статусу
Показує детальний статус всіх сервісів.

```bash
./status.sh
```

**Що показує:**
- ✅ Статус backend процесів
- ✅ Статус frontend процесів
- ✅ Статус Vite процесів
- ✅ Зайняті порти (3000-3010)
- ✅ Доступність API endpoints
- ✅ Статус бази даних
- ✅ Корисні команди

**Результат:**
```
================================
📊 TalentFlow Status Check
================================

[INFO] Перевіряю Node.js процеси...

🔧 Backend процеси:
✅ Backend запущений
  michaelilyash 12345 0.0 0.1 34525020 17032 npm run dev

🌐 Frontend процеси:
✅ Frontend запущений
  michaelilyash 12346 0.0 0.4 55121512 68360 vite

🔌 Порти (3000-3010):
✅ Порт 3000: node (PID: 12345)
✅ Порт 3001: node (PID: 12346)
⚪ Порт 3002: вільний
...

🌐 Доступність сервісів:
✅ Backend API: http://localhost:3000
  ✅ Health check: OK
  ✅ Swagger: http://localhost:3000/api-docs
✅ Frontend: http://localhost:3001
  ✅ Frontend відповідає

🗄️ База даних:
✅ PostgreSQL: доступний на localhost:5432

🛠️ Корисні команди:
  Запустити: ./start.sh
  Зупинити:  ./stop.sh
  Статус:    ./status.sh
  API тест:  curl http://localhost:3000/health
  Сайт:      http://localhost:3001

================================
📊 Status Check Complete
================================
```

## 🎯 Швидкий старт

### 1. Перший запуск
```bash
# Клонуйте репозиторій
git clone <repository-url>
cd talentflow

# Запустіть проект
./start.sh
```

### 2. Щоденне використання
```bash
# Запуск
./start.sh

# Перевірка статусу
./status.sh

# Зупинка
./stop.sh
```

### 3. Розробка
```bash
# Запуск в режимі розробки
./start.sh

# В іншому терміналі - перевірка статусу
./status.sh

# Тестування API
curl http://localhost:3000/health

# Відкриття сайту
open http://localhost:3001
```

## 🔧 Налаштування

### Змінні середовища
Скрипти автоматично створюють `.env` файли з прикладів:

**Backend (.env):**
```env
PORT=3000
DATABASE_URL=postgresql://username:password@localhost:5432/talentflow
JWT_SECRET=your-super-secret-jwt-key
NODE_ENV=development
```

**Frontend (.env):**
```env
VITE_API_URL=http://localhost:3000
VITE_APP_NAME=TalentFlow
```

### Порти
Скрипти автоматично знаходять вільні порти:
- **Backend**: 3000 (або наступний вільний)
- **Frontend**: 3001 (або наступний вільний)

### База даних
Для роботи потрібен PostgreSQL. Можна запустити:

**Через Docker:**
```bash
docker run --name talentflow-postgres \
  -e POSTGRES_PASSWORD=password \
  -e POSTGRES_DB=talentflow \
  -p 5432:5432 \
  -d postgres:14
```

**Локально:**
```bash
# macOS (Homebrew)
brew install postgresql
brew services start postgresql

# Ubuntu/Debian
sudo apt install postgresql postgresql-contrib
sudo systemctl start postgresql
```

## 🛠️ Розширені команди

### Логи
```bash
# Backend логи
tail -f backend/logs/app.log

# Frontend логи (в терміналі де запущений)
# Логи показуються автоматично

# Всі логи
./status.sh
```

### Тестування
```bash
# Тест API
curl http://localhost:3000/health

# Тест Swagger
curl http://localhost:3000/api-docs

# Тест frontend
curl http://localhost:3001/
```

### Перезапуск
```bash
# Повний перезапуск
./stop.sh && ./start.sh

# Тільки backend
cd backend && npm run dev

# Тільки frontend
cd web && npm run dev
```

## 🚨 Вирішення проблем

### Проблема: "Permission denied"
```bash
chmod +x *.sh
```

### Проблема: "Node.js не знайдено"
```bash
# Встановіть Node.js 20+
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
nvm install 20
nvm use 20
```

### Проблема: "Порт зайнятий"
```bash
# Знайдіть процес
lsof -i :3000

# Зупиніть процес
kill -9 <PID>

# Або використайте інший порт
PORT=3005 ./start.sh
```

### Проблема: "База даних недоступна"
```bash
# Перевірте статус PostgreSQL
pg_isready -h localhost -p 5432

# Запустіть PostgreSQL
brew services start postgresql  # macOS
sudo systemctl start postgresql  # Linux
```

## 📱 Результат

Після успішного запуску ви отримаєте:

- 🌐 **Frontend**: http://localhost:3001
- 🔧 **Backend API**: http://localhost:3000
- 📚 **Swagger документація**: http://localhost:3000/api-docs
- 🏥 **Health check**: http://localhost:3000/health

**TalentFlow готовий до роботи! 🚀**

---

*Скрипти створені для зручності розробки TalentFlow*
*Where Talent Flows Naturally*
